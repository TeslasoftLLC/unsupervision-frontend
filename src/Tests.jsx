import React from 'react';

function Tests(props) {
    return (
        <div className={"pp"}>
            <h2 className={"tab-title"}>Tests</h2>
        </div>
    );
}

export default Tests;